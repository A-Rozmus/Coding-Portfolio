#include "bitwise_operations.h"

bool Emptiness(int zbior)
{
    if(zbior != 0)
        return false;
    else
        return true;
}

bool Nonempty(int zbior)
{
    if(zbior != 0)
        return true;
    else
        return false;
}
